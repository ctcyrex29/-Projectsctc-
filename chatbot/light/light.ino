const int lightPin = 13;

void setup() {
  pinMode(lightPin, OUTPUT);
  digitalWrite(lightPin, LOW);  // Turn off the light initially
}

void loop() {
  // Arduino code runs continuously, but we are not using it in this example
}
